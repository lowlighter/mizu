{
  "cleanUrls": true,
  "trailingSlash": true,
  "framework": null,
  "installCommand": "curl -fsSL https://deno.land/install.sh | sh -s -- -y && cp /vercel/.deno/bin/deno /usr/local/bin",
  "buildCommand": "deno task build",
  "outputDirectory": ".pages"
}
